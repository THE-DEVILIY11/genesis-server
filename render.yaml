services:
  - type: web
    name: realtime-sync-hub
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: node server.js
    envVars:
      - key: OP_SECRET
        sync: false
      - key: TG_TOKEN
        sync: false
      - key: TG_ADMIN
        sync: false